# Debda Tournament

Dead by Daylight tournament app with persistent data, login-aware pages, and scorekeeping planned around the Debda event.

## Development

Install dependencies:

```sh
npm install
```

Start the app:

```sh
npm start
```

Run database migrations:

```sh
npm run migrate:dev
```
